import 'dart:io';
import 'dart:ui' as ui;

class FaceCropper {
  /// Crop the face area from the image using bounding box. This is a stub.
  Future<File> cropToIdFormat(File image, ui.Rect boundingBox) async {
    // Implement actual cropping with `image` package or platform APIs.
    return image;
  }
}
